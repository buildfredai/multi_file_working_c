# Multi File Working in C 🚀

This repository demonstrates **how to work with multiple `.c` and `.h` files in C**, a core concept for building **large, structured, and scalable C programs**.

It is created to help beginners understand **file separation, function sharing, and compilation using GCC**.

---

## 📂 Project Structure

```text
multi_file_working_c/
│
├── main.c          # Entry point of the program
├── math.c          # Function definitions
├── math.h          # Function declarations (header file)
├── utils.c         # Utility functions
├── utils.h         # Utility declarations
└── README.md
